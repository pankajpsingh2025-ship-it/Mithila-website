# React 19 vs. an old peer range on the (unused) @studio-freight/react-lenis dep.
# The lockfile was resolved this way; keep npm ci / npm install non-strict so CI
# installs match local.
legacy-peer-deps=true
